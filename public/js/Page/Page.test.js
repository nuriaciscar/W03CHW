describe( "Given a Page class", () => {
  describe("When it receives a div container, a 'test' class and a 'p' tag", () =>{
})



// describe("Given a component class", () => {
//   describe("When it receives a div container, a 'test' class and a 'p' tag", () =>{
//     test ("Then it should render a p element with a 'test' class inside the div container", () =>{

//       //Arrange
//       const container= document.createElement("div");
//       const className= "test";
//       const tag= "p";

//       //Act
//      new Component(container, className, tag);
//      console.log(container. querySelector("p.test"));
//       const pFound = container.querySelector("p.test");

//       //Assert
//       expect(pFound).not.toBeNull();
//       //Espero que este div que he creado tenga un elemento que sea un p con clase test
//   }) 
//   })
// }