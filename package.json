{
  "name": "chalenge-finde3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --watchAll --collect-coverage",
    "sass": "sass src/styles.scss public/css/styles.css ",
    "sass-dev": "sass --watchAll src/styles.scss public/css/styles.css "
  },
  "babel": {
    "env": {
      "test": {
        "plugins": [
          "@babel/plugin-transform-modules-commonjs"
        ]
      }
    }
  },
  "jest": {
    "testEnvironment": "jsdom"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.25.2",
    "sass": "^1.43.2"
  }
}
